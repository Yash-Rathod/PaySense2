version: "3.9"

services:
  producer:
    build: ./producer
    image: paysense-producer:dev
    command: ["--rows", "100", "--fraud-rate", "0.1", "--tps", "10"]

  consumer:
    build: ./consumer
    profiles: ["app"]
