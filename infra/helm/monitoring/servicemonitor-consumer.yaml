apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: consumer
  namespace: paysense
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: consumer
  endpoints:
    - port: metrics
      interval: 15s
      path: /metrics
